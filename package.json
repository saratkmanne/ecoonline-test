{
  "name": "trpo-assessment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-chrome": "cypress run -b chrome --headed -s './cypress/e2e/*.spec.cy.js'",
    "test-chrome-headless": "cypress run -b chrome --headless -s './cypress/e2e/*.spec.cy.js'",
    "test-firefox": "cypress run -b firefox --headed -s './cypress/e2e/*.spec.cy.js'",
    "test-electron": "cypress run -b electron --headed -s './cypress/e2e/*.spec.cy.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.2.0",
    "cypress-mochawesome-reporter": "^3.6.1",
    "xpath_cypress": "^1.0.2"
  }
}
